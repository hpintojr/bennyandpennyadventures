# Admin Assets

Drop the sidebar bear mascot PNG here when it is ready:

```txt
public/admin/bear-sidebar-placeholder.png
```

The Payload admin theme already reserves a bottom-sidebar mascot area and references `/admin/bear-sidebar-placeholder.png` as a replaceable background image. Until the PNG is added, the sidebar shows a simple bear placeholder.
